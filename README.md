# Demo assets

Runtime walkthrough media for PRs #4323 (Observation Mode) and #4324 (Split View),
captured headlessly via xvfb + Playwright. Kept on a standalone branch so the
feature PR diffs stay free of binaries.
